# 🚀 Firebase Firestore 설정 및 오류 해결 가이드

이나즈마 위키 서비스에서 전술, 포메이션, 커뮤니티 글 저장 시 발생하는 오류를 해결하기 위한 가이드입니다. 
파이어베이스 Firestore의 **보안 규칙**과 **복합 인덱스**를 설정해 주시면 정상적으로 저장 기능이 동작합니다.

---

## 1. Firestore 보안 규칙(Security Rules) 적용 방법

파이어베이스에서 기본으로 제공하는 규칙이 만료되었거나 쓰기가 허용되지 않아 에러가 발생할 수 있습니다. 

1. [Firebase Console](https://console.firebase.google.com/)에 접속하여 본인의 프로젝트를 선택합니다.
2. 좌측 메뉴에서 **Build (빌드) > Firestore Database**를 클릭합니다.
3. 상단의 탭 중 **Rules (규칙)**를 선택합니다.
4. 프로젝트 루트 폴더에 생성된 `firestore.rules` 파일의 전체 내용을 복사하여 기존 규칙 창에 그대로 덮어씁니다.
5. 우측 상단의 **Publish (게시)** 버튼을 클릭하여 규칙을 적용합니다.

---

## 2. 복합 인덱스(Composite Index) 생성 방법

내 전술 및 포메이션 목록을 가져올 때 `userUid`(작성자) 필터와 `createdAt`(생성일자) 정렬을 복합적으로 사용하므로, 파이어베이스에 복합 인덱스를 수동으로 생성해 주어야 쿼리가 정상 작동합니다.

### 2.1 콘솔 에러 링크를 통한 자동 생성 (가장 추천)
1. F12를 눌러 **브라우저 개발자 도구 (Console)**를 엽니다.
2. 전술판 페이지(`/tactics`)로 이동 시 콘솔에 아래와 같은 에러 로그가 찍혀 있는지 확인합니다.
   > `FirebaseError: The query requires an index. You can create it here: https://console.firebase.google.com/...`
3. 에러 로그에 포함된 **해당 URL 링크를 클릭**합니다.
4. 파이어베이스 콘솔로 이동되며 자동으로 인덱스 설정 창이 팝업됩니다. **인덱스 만들기** 버튼을 클릭하고 2~3분간 기다립니다.

### 2.2 수동으로 인덱스 생성
에러 링크가 보이지 않거나 직접 수동 생성하고 싶으시다면 아래 정보를 입력하여 생성할 수 있습니다.

1. **Build > Firestore Database > Indexes (색인)** 탭으로 이동합니다.
2. **복합 색인** 항목에서 **색인 추가** 버튼을 클릭합니다.
3. 아래 표를 참조하여 **두 개의 복합 색인**을 각각 추가해 줍니다.

#### 💡 색인 1 (전술판 목록용)
- **컬렉션 ID**: `tactics`
- **필드 구성**:
  1. `userUid` : `오름차순` 또는 `내림차순`
  2. `createdAt` : `내림차순`
- **쿼리 범위**: `컬렉션`

#### 💡 색인 2 (포메이션 좌표 목록용)
- **컬렉션 ID**: `formations`
- **필드 구성**:
  1. `userUid` : `오름차순` 또는 `내림차순`
  2. `createdAt` : `내림차순`
- **쿼리 범위**: `컬렉션`

> [!NOTE]
> 인덱스는 생성 버튼 클릭 후 상태가 `준비 중`에서 `사용 설정됨`으로 변경되는 데 약 2~3분이 소요됩니다. 인덱스가 활성화되면 클라우드 목록이 실시간으로 로드되기 시작합니다.
